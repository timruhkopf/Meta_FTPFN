nll_avg_ckpt:
  _target_: ppfn.trainer.callbacks.checkpoint_callback.CheckpointCallback
  save_dir: ${hydra:run.dir}/checkpoints/
  name: "best_avg_nll_CA_bench128"
  mode: "min"
  _convert_: object
  monitor: # average over these models
    - "nll/C-A:pd1_tabular_128"
    - "nll/C-A:taskset_tabular_128"
    - "nll/C-A:lcbench_tabular_128"
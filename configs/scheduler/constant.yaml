# The previous version of this file (`type: "constant"`) had no `_target_` and
# was never actually instantiable via `instantiate(cfg.scheduler)` in
# src/train.py — fixed here. `factor: 1.0` means the LR is never scaled, i.e.
# genuinely constant, regardless of `total_iters`.
_target_: torch.optim.lr_scheduler.ConstantLR
_partial_: true
factor: 1.0
total_iters: 0

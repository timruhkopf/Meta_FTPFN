# Default trainer configuration
epochs: 300
steps: 63
trainer_class:
  _target_: ppfn.trainer.PPFNTrainer
  use_amp: false
  grad_clip: 1.0
  aggregate_k_gradients: 1
  verbose: true
  callbacks:
    - _target_: ppfn.model.mymodel.cross_fusion.CrossFusionLossCallback

    - _target_: ppfn.trainer.callbacks.mfpbench_callback.MetaTestBenchmarkCallback
      benchmark_name: lcbench_tabular
      data_path: ${oc.env:DATADIR}/validation
      single_eval_pos: 64
      n_folds: 3
      device: ${device}

  experiment_name: ${experiment_name}



# Lab book

Running, append-only log of empirical findings — distinct from
`docs/milestones/` (planned work) and `docs/reviews/` (point-in-time audits).
See `.claude/rules/labbook.md` for the convention. Never edit a past entry to
"correct" it; superseding findings get a new entry that links back.

## Entries

| Date | Entry | Summary |
|---|---|---|
| 2026-09-10 | [arch_verification OOM: uncapped cloud sizes, not batch size](2026-09-10-arch-verification-oom-cloud-size.md) | Three OOMs while cutting batch_size (8→2) never fixed it; root cause was the registration prior's uncapped n_b (up to 1024) driving O(n²) pooled self-attention, plus role randomization letting either cloud reach the uncapped range. |
| 2026-09-10 | [categorical KL NaN from float32 softmax underflow](2026-09-10-categorical-kl-nan-underflow.md) | `arch_verification` crashed with a NaN loss ~7800 steps in. Root cause: `_categorical_kl`'s `p_t * (log_p_t - log_p_s)` has two `0 * inf`-shaped underflow failure modes. Fixed with a log-prob floor + explicit zero-mass mask, shared by both `arch_verification_loss.py` and `registration_loss.py`. |
| 2026-09-10 | [HPO-warp noise floor off by n_seeds](2026-09-10-hpo-warp-noise-floor-off-by-n-seeds.md) | First HPOBench-`lr` warp-complexity sweep used the single-observation seed variance as the noise floor instead of the variance of the mean it was actually being compared against — 5x too lenient, systematically under-selecting elbow capacity. Fixed (`noise_floor / n_seeds`), first 151 pairs discarded, sweep relaunched clean. |
| 2026-09-10 | [Scaling HPO-warp to scattered, higher-d benchmarks](2026-09-10-hpo-warp-scaling-to-scattered-higher-d-benchmarks.md) | Extending to LCBench (7D)/TaskSet (8D) surfaced four bugs (grid-cost explosion in severity calc, `log(0)` from epoch-0, a kernel bandwidth formula that oversmooths in higher d, mismatched surviving-config sets crashing `spearmanr`) — all fixed and verified. TaskSet's heavy-tailed loss scale broke MSE-based warp fitting; deferred rather than patched, LCBench covers this round's "bigger search space + fidelity" ask instead. |
| 2026-09-10 | [Chain script CLI mismatch after mid-run redeploy](2026-09-10-hpo-warp-chain-script-cli-mismatch.md) | Adding a required `--family` flag to `run_pairs.py` mid-sweep broke a *queued* chain script's hardcoded old-style invocation for `xgb`/`nn` — both failed instantly, but the loop had no error check and reported "finished" anyway, nearly letting a dependent script proceed on a false premise. Caught by implausible 2-second "completion" times. |
| 2026-09-11 | [Sequential h-then-T fit understated severity ~8x](2026-09-11-hpo-warp-sequential-h-then-t-was-biased.md) | User-prompted check: fitting `h` once at the identity correspondence, frozen while `T` is fit, biases severity low (0.36 vs true 2.78 on a synthetic ground-truth pair). First fix attempt: alternation (ACE-style refitting) — a real improvement, later superseded (see below). |
| 2026-09-11 | [Velocity-field collapse fixed by restarts](2026-09-11-hpo-warp-velocity-field-collapse-fixed-by-restarts.md) | User noticed the synthetic case study's recovered warp looked severely underfit. Root cause: zero-init velocity fields can get stuck near the identity depending on random init; more optimization steps doesn't reliably fix it (can make it worse). Fixed with 3 random restarts per rung, selected by training loss — also made fits exactly reproducible (were seed-dependent before). This fix stands independent of the h-family change below. |
| 2026-09-11 | [Isotonic h retracted: identifiability confound, not optimization bug](2026-09-11-hpo-warp-h-family-constrained-to-yeojohnson.md) | Alternation (previous entry) was treating a symptom: an unconstrained isotonic `h` can absorb `T`'s job regardless of how it's refit. Fixed by constraining `h` to `a*YeoJohnson(y;lambda)+b` (3 params, nests exact affine at lambda=1) — reasoned from what real HPO y-distortion needs (shift/scale mandatory, at most one skew/boundedness shape parameter). Single pass, no alternation, now recovers severity within the right order of magnitude on the synthetic case (was 8x off). Alternation code removed entirely, not kept as a fallback. |
| 2026-09-11 | [Boundary dead-zone in warp fitting, fixed with a penalty term](2026-09-11-hpo-warp-boundary-penalty.md) | User-noticed artifact: the true-vs-recovered warp plot showed inflated displacement near plot boundaries. Root cause: `clamp01` before reading B's surface makes the loss flat for any raw output already outside `[0,1]^d`, so nothing shapes the field past the edge (6.6% of probe points affected, ~2x displacement near boundaries). Fixed with a boundary penalty term; excursion fraction dropped to 1.5% and fit quality improved (residual/noise-floor 4.1→3.68), but a same-day correction retracted the initial "~4% off ground truth" severity number — it came from a verification script whose RNG stream diverged from the notebook's, not from the fix itself; net effect on severity recovery is flat, not improved. |
| 2026-09-11 | [\|∇f\|-identifiability of T: real but partial, cuts both ways for HPO relatedness](2026-09-11-hpo-warp-gradient-identifiability-tradeoff.md) | Same plot's lower-right quadrant under-registered the true warp; `docs/ROADMAP.md`'s "T is only identified where \|∇f\| is appreciable" argument explains it directionally (13% lower gradient) but not fully (2.5x displacement gap — the rest is kernel-placement luck). For the relatedness *metric* specifically, this identifiability gap is a double-edged sword: it may usefully align with what BO cares about (steep regions), but it also means low-severity readings in shared-flat regions are under-evidenced, not confirmed, and can hide exactly the divergent tail behavior relatedness is meant to catch. |
| 2026-09-14 | [Citation check: Pushak & Hoos, van Rijn & Hutter, μTransfer](2026-09-14-citation-check-pushak-vanrijn-mutransfer.md) | Checked three landscape-structure citations before shipping them to `REFERENCES.md`. Pushak & Hoos over-stated ("benign unimodal landscapes" was slice-based, algorithm-configuration, failure-to-reject evidence — hedged in the ladder log, and a table shows unimodality is load-bearing for less of the framework than assumed). Van Rijn & Hutter cited backwards (their result is stable cross-task importance, i.e. *supports* a shared active subspace; the real geometric obstruction is "importance crossing zero" changing whether the good region touches a box face, independent of that paper). μTransfer restated properly into a reparameterization-conjugation argument (`T ↦ RTR⁻¹`) plus a new, not-yet-built idea: a coordinate-free relatedness measure via minimum energy over a reparameterization group. |
| 2026-09-14 | [LUPI-distilled alignment-aware multi-task PFN: first build](2026-09-14-lupi-alignment-pfn-build.md) | Built the LUPI/JEPA-thread build spec as a new, self-contained `ppfn.prior.lupi`/`ppfn.model.lupi`/`ppfn.loss.lupi_loss`/`ppfn.trainer.lupi_trainer` stack, reusing `ppfn.prior.registration.warp` for T and `ppfn.model.pfn.pfn.PFNBlock`/`BarDistribution` for the alignment stack and NLL. Local smoke tests pass (rho=0 invariant exact, gradients flow, padding/permutation invariance hold, a 2-epoch debug run trains end to end and logs to a separate MLflow experiment). Real-sized run not yet launched. |
| 2026-09-14 | [LUPI prior bottleneck: warp rejection-sampling grid, not the GPU](2026-09-14-lupi-prior-warp-grid-speedup.md) | ulysses run was CPU-bound, not stalled: cProfile localized 84% of per-item cost to sample_warp_pair's log-Jacobian rejection check (a grid_n^d finite-difference grid, 3125 pts at d=5). LUPI-local grid_n 5->4 override (registration prior untouched) cut it 3.1x for a modest, deliberately-chosen loosening of the accept/reject band estimate (grid_n=3 gave 12.3x but underestimated the band ~40%, rejected). |
| 2026-09-15 | [LUPI bounds PFN, query-source/rho stratification, and a val-batch bug](2026-09-15-lupi-bounds-and-stratified-diagnostics.md) | Stratified the oracle-student gap by query source and rho against the existing checkpoint: gap grows 0→0.029 from rho=0 to rho~0.3 then plateaus (not monotone with difficulty), near-B queries get the best NLL of all three buckets but the smallest gap — working hypothesis is value-matching (identical in both modes) is carrying most of the prediction quality. Found and fixed a real bug: `LUPITrainer.__init__` built its validation batch without the run's own n_a_range/n_b_range, so every prior run's own val/loss/* was on a mismatched distribution. Added BoundsPFN (one checkpoint, severed-mode toggle gives both lower/upper bounds) and a standing-view bounds report; launched both the bounds-PFN run and a no-distillation control run, chained sequentially on ulysses. |
| 2026-09-15 | [B_inA visualization check, and a naive-pooling failure mode it surfaced](2026-09-15-lupi-visualization-b-inA-check.md) | User flagged B's points looking identical across notebook rows 2-4; verified numerically first — genuinely nonzero (`\|x_b_inA - x_b\|` mean 0.032) but too small to see by eye at that draw's rho/s_max, not a bug (both plotting and model-feeding code already used x_b_inA consistently). Fixed by drawing B's own frame explicitly in every row and picking a draw with a visibly larger warp. Along the way, found the off-the-shelf reused-PFN's "upper bound" can score *worse* than its own "lower bound" when pooling A_ctx (small, acquisition-biased quantile scale) with B_inA (large, uniform quantile scale) — a plausible illustration of the core premise (naive pooling isn't automatically safe under a scale mismatch), not yet confirmed beyond one draw. |
